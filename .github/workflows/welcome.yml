#
# @Project: @cldmv/droidsock
# @Filename: /.github/workflows/welcome.yml
# @Copyright: Copyright (c) 2013-2026 Catalyzed Motivation Inc. All rights reserved.
#

name: 👋 Welcome Contributor

# SECURITY NOTE: pull_request_target runs in the BASE repo's context with
# WRITE permissions. SAFE — no checkout of PR head, no PR-supplied code
# execution, REST-API-only (read prior interactions, post a comment).
on:
    issues:
        types: [opened]
    pull_request_target:
        types: [opened]

permissions:
    issues: write
    pull-requests: write

jobs:
    welcome:
        uses: CLDMV/.github/.github/workflows/reusable-welcome.yml@v4
        secrets:
            BOT_APP_CLIENT_ID: ${{ secrets.CLDMV_BOT_APP_CLIENT_ID }}
            BOT_APP_PRIVATE_KEY: ${{ secrets.CLDMV_BOT_APP_PRIVATE_KEY }}
